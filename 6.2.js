/* Exercise 6.2
Create a class name Rectangle with attributes topLeftXPos, topLeftYPos, width and lenght.
 Add a method named collides(otherRectangle) that returns true only if the current rectangle 
 collides with otherRectangle.

Test the collides(otherRectangle) method by using multiple test cases.*/

class Rectangle{
    constructor(topLeftXPos,topLeftYPos,width,length){
       this.topLeftXPos=topLeftXPoss;
       this.topLeftYPos=topLeftYPos;
       this.width=width;
       this.length=length;
    }
}

let 


/* let otherRectangle=new Rectangle(topLeftXPos2,topLeftYPos2,width2,length2);
    collides(otherRectangle){
        if(topLeftXPos2>this.topLeftXPos && topLeftXPos2<this.topLeftXPos+this.length && topLeftYPos2>this.topLeftYPos && topLeftYPos2<this.topLeftYPos+this.width){
            return true;
        } else if(topLeftXPos2+length2>this.topLeftXPos && topLeftXPos2+length2<this.topLeftXPos+this.length && topLeftYPos2+width2>this.topLeftYPos && topLeftYPos2+width2<this.topLeftYPos+this.width){
            return true;
        } else {
            return false;
        }
    }

let rectangle1= new Rectangle(8,8,3,6);
rectangle1.collides(new Rectangle(9,5,5,4)); */
 